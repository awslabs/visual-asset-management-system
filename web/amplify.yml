version: 1
frontend:
    phases:
        preBuild:
            commands:
                - npm install -g npm@11.19.1
                - export NODE_OPTIONS=--max-old-space-size=8192
                - npm ci
        build:
            commands:
                - npm run build
    artifacts:
        baseDirectory: dist
        files:
            - "**/*"
    cache:
        paths:
            - node_modules/**/*
