# Dependency intake is pinned by the committed package-lock.json and gated by the release grype
# scan; min-release-age needs a newer npm than the pinned Node 22 toolchain guarantees -- tracked as
# a 2.6.1 follow-up.
# nosemgrep: package_managers.npm.npm-missing-minimum-release-age.npm-missing-minimum-release-age
engine-strict = true