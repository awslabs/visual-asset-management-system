build
node_modules
cdk.out
# `cdk synth -o <dir>` / `cdk deploy -o <dir>` write assemblies to a suffixed directory, which
# .gitignore already covers as cdk.out.* . Without the same pattern here, one such directory
# makes prettier-check report ~1900 generated CloudFormation templates as unformatted.
cdk.out.*
infra/cdk.context.json
.mypy_cache
.pytest_cache
ash_cf2cdk_output
ash
backend/.venv
.venv
documentation/docusaurus-site/.docusaurus
web/dist
web/customInstalls/*/source
web/customInstalls/*/dist
web/public/potree_libs
web/public/viewers
web/test-results
web/playwright-report
/docs
.plans
web/e2e/.auth
web/e2e/.report
*.yaml
*.patch
# Gitignored local scratch that prettier would otherwise report forever (it never ships).
backendPipelines/3dRecon/splatToolbox/container/LOCAL_DEBUG_README.md
# Model-facing prompt text of the Video SOP/BOM container. Its unit tests pin the bytes (list
# markers, blank lines, the fenced operator block) and a formatter's reflow would change what the
# model reads. Path-anchored with a trailing slash so it matches this one directory.
backendPipelines/genAi/videoSopBom/container/video_sop_bom_pipeline/prompts/
