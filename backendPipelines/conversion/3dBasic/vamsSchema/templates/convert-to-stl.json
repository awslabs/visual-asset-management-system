{
    "templateId": "convert-to-stl",
    "templateName": "Convert to STL",
    "description": "Convert a 3D mesh to STL format using the Trimesh library.",
    "configFormat": "json",
    "configBody": "{\"outputType\":\".stl\"}",
    "allowCustomEdit": false,
    "inputInstructions": "Converts a 3D model to STL (.stl). Select the source model as the input file; the converted file is written back to the asset.\n\nThis pipeline reads no asset or file metadata — everything it needs comes from the selected file and this template."
}
