{
    "templateId": "convert-to-gltf",
    "templateName": "Convert to GLTF",
    "description": "Convert a 3D mesh to GLTF format using the Trimesh library.",
    "configFormat": "json",
    "configBody": "{\"outputType\":\".gltf\"}",
    "allowCustomEdit": false,
    "inputInstructions": "Converts a 3D model to glTF (.gltf). Select the source model as the input file; the converted file is written back to the asset.\n\nThis pipeline reads no asset or file metadata — everything it needs comes from the selected file and this template."
}
