{
    "templateId": "convert-to-obj",
    "templateName": "Convert to OBJ",
    "description": "Convert a 3D mesh to OBJ format using the Trimesh library.",
    "configFormat": "json",
    "configBody": "{\"outputType\":\".obj\"}",
    "allowCustomEdit": false,
    "inputInstructions": "Converts a 3D model to Wavefront OBJ (.obj). Select the source model as the input file; the converted file is written back to the asset.\n\nThis pipeline reads no asset or file metadata — everything it needs comes from the selected file and this template."
}
