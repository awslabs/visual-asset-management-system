{
    "templateId": "cad-step-agent-generate",
    "templateName": "Generate a new STEP file",
    "description": "Create a brand-new STEP file on the chosen output asset from a design description, optionally researching reference designs online.",
    "configFormat": "json",
    "configBody": "{\"mode\":\"generate\",\"prompt\":\"{{PROMPT}}\",\"outputFilename\":\"{{OUTPUT_FILENAME}}\",\"outputFilenamePrefix\":\"{{OUTPUT_FILENAME_PREFIX}}\",\"allowInternetResearch\":{{ALLOW_INTERNET_RESEARCH}},\"modelProvider\":\"{{MODEL_PROVIDER}}\",\"modelId\":\"{{MODEL_ID}}\",\"maxAttempts\":{{MAX_ATTEMPTS}},\"designName\":\"{{DESIGN_NAME}}\"}",
    "tagSchema": [
        {
            "tagKey": "PROMPT",
            "type": "string",
            "required": false,
            "label": "Design instruction",
            "description": "What the agent should do. For a modify run, describe the change to the input STEP file (for example \"add four M3 clearance holes on the base plate at 40 mm spacing\"). For a generate run, describe the part or assembly to create. The agent reports anything it could not do in the run's report. Left unchanged, the default instruction produces a sample mounting plate.",
            "default": "Create a 100 x 60 x 10 mm rectangular mounting plate with four 4.5 mm clearance holes for M4 screws, each inset 8 mm from its corner."
        },
        {
            "tagKey": "OUTPUT_FILENAME",
            "type": "string",
            "required": false,
            "default": "",
            "label": "Output file name",
            "description": "Optional file name for the result (no folders). Empty keeps the input file's name for a modify run, which writes a new version of that file; empty on a generate run produces a name from the design name or prompt plus a timestamp. The STEP extension is kept or added."
        },
        {
            "tagKey": "OUTPUT_FILENAME_PREFIX",
            "type": "string",
            "required": false,
            "default": "",
            "label": "Output file name prefix",
            "description": "Optional prefix prepended to the resolved file name (for example \"v2-\"), so a modify run writes a separate file instead of a new version of the input."
        },
        {
            "tagKey": "ALLOW_INTERNET_RESEARCH",
            "type": "boolean",
            "required": false,
            "default": true,
            "label": "Allow internet research",
            "description": "Let the agent search the web and fetch pages to research the design (reference dimensions, published board layouts). The deployment can disable research for every run; this setting then has no effect."
        },
        {
            "tagKey": "MODEL_PROVIDER",
            "type": "enum",
            "required": false,
            "default": "bedrock",
            "enumValues": ["bedrock", "openai"],
            "label": "Model provider",
            "description": "bedrock uses the deployment's Amazon Bedrock model (Anthropic Claude by default); openai uses the deployment's OpenAI model and requires the OpenAI API key secret to be configured."
        },
        {
            "tagKey": "MODEL_ID",
            "type": "string",
            "required": false,
            "default": "",
            "label": "Model ID override",
            "description": "Optional model id for the chosen provider; empty uses the deployment default."
        },
        {
            "tagKey": "MAX_ATTEMPTS",
            "type": "integer",
            "required": false,
            "default": 4,
            "label": "Maximum script attempts",
            "description": "How many generated-script attempts the agent may make before the run is failed (1-10). Each attempt validates the produced STEP file and feeds the result back to the agent."
        },
        {
            "tagKey": "DESIGN_NAME",
            "type": "string",
            "required": false,
            "default": "",
            "label": "Design name",
            "description": "Optional short name for the design, used for the generated file name (for example \"Jetson Nano carrier board\" becomes jetson-nano-carrier-board-<timestamp>.step) when no output file name is set."
        }
    ],
    "allowCustomEdit": true,
    "inputInstructions": "No input file is selected for this template; choose the output asset the new STEP file is written to. Describe the part or assembly in the design instruction. With internet research allowed, the agent may look up reference dimensions or published designs and lists the sources it used; anything it could not find or reproduce is recorded in the run's report."
}
